name: Test

on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Build the stack
        run: docker-compose up -d
        
      - name: Run unit tests
        run: python3 manage.py test core
        working-directory: ./api
